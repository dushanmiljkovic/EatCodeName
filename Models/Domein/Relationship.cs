namespace Models.Domein
{
    public enum DisheDrink
    {
        GoesWith,
        Never
    }

    public enum DisheTypeEnum
    {
        Side,
        Main,
        Dessert
    }

    public enum DishCourse
    {
        Is
    }

    // with procent
    public enum DisheIngredient
    {
        Contain
    }
}